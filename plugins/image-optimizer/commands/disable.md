---
name: disable
description: Disable the image optimizer without uninstalling
---

# Disable Image Optimizer

Set `enabled` to `false` in `${CLAUDE_PLUGIN_DATA}/config.json` (create with defaults if missing).

Confirm: "Image optimizer **disabled**. MCP images will pass through at original size."
