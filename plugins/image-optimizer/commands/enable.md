---
name: enable
description: Enable the image optimizer
---

# Enable Image Optimizer

Set `enabled` to `true` in `${CLAUDE_PLUGIN_DATA}/config.json` (create with defaults if missing).

Confirm: "Image optimizer **enabled**. MCP images will be automatically downsized."
