After your first substantive response in this session, suggest a session name so the user can identify it later. Output a single line at the end of your response in this exact format:

`/name suggested-slug-name`

The slug should be 3-6 words, lowercase, hyphenated, describing what the session is about (e.g. `fix-auth-timeout-bug`, `build-session-namer-plugin`, `refactor-payment-webhooks`). Only do this once — never suggest a name again after the first response. Skip this if the session already has a name.
